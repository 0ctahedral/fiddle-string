def foo(x):
  let f = (lambda (p): x + p) in
  x
and def bar(x):
  let f = (lambda (p): x + p) in
  let g = (lambda (p): x + p) in
  print(f(g(x)))

let t = (4, (5, 6)) in
            begin
              t[0] := 7;
              foo(t)
            end
